# CareerTrack 开发约束

- 保留工作树中与当前任务无关的修改；不要执行 reset、revert 或覆盖他人改动。
- 未经明确要求，不合并分支、不 push。完成实现后提交前先确认差异和测试结果。
- 新增或修改 API 时，复用现有校验与错误响应约定；服务端必须校验资源归属，写操作需要时使用 revision 防止并发覆盖。
- 日期字段若表示日期而非时刻，对外统一为 `YYYY-MM-DD`；跨 SQLite/PostgreSQL 时显式处理类型和时区。时刻对外使用 UTC ISO 8601。
- 新增受保护页面时，先等待会话解析完成，再根据认证状态跳转；不要依据 Zustand 的初始值提前重定向。
- 数据库已发布的迁移不可修改；用新迁移演进 schema，并同步 SQLite、PostgreSQL、schema 文档和相关集成测试。
- 读取失败必须与空数据区分，并提供可重试的用户反馈；不要静默吞掉请求失败。
- 代码变更至少运行 lint、单测和构建（类似 Dockerfile 可以不管）；涉及接口或交互时补充相关 E2E。涉及 PostgreSQL 时更新并运行集成测试（环境可用时）。
- 使用 Ant Design 时不得新增已弃用的 API；出现控制台弃用警告须改用官方推荐属性，并保留原有交互与视觉效果。
- API、schema 或用户可见行为变化时，同步更新 `docs/API.md`、`docs/DATABASE.md`、`docs/CHANGELOG.md` 和项目评审记录（仅更新受影响文档）。
- 仅为非显然的约束、并发或兼容性逻辑写注释；不要添加重复代码本意的注释。
- 每次 push 都要更新一次版本号
